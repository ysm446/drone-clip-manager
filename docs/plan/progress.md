# progress — flight-cut 進捗

作成日時: 2026-07-08 12:29
更新日時: 2026-07-08 12:29

現在の進捗・完了済み・未完了・注意点をまとめる。作業のたびに更新する。

## 現状サマリ

- フェーズ: **Phase 0（設計・ドキュメント整備）**。実装コードは未着手。
- リポジトリは docs 中心。アプリ本体（Electron + React）のスキャフォールドはまだ無い。

## 完了済み

- [x] `docs/spec.md` — 全体仕様（目的 / スコープ / 技術スタック / アーキ / データモデル / キーフレーム & ロスレス切り出し / UI / フロー / 拡張点 / フェーズ）
- [x] `docs/spec.md` にストレージ設計を追記（ルートフォルダをユーザー指定、メタデータはルート直下 `.flightcut/` に作成、相対パス管理）
- [x] `AGENTS.md` — エージェント運用ルール
- [x] `CLAUDE.md` — Claude Code 向け入口（`@AGENTS.md` インポート + プロジェクト概要）
- [x] `docs/plan/goals.md` / `plan.md` / `progress.md` — 計画ドキュメントを記入

## 未完了（次にやること）

### Phase 1 — ライブラリ基盤（未着手）
- [ ] Electron + React プロジェクトのスキャフォールド、`package.json`（`version` 基準）
- [ ] `better-sqlite3` 導入と DB スキーマ作成（`videos` / `keyframes` / `segments`）
- [ ] ルートフォルダ指定ダイアログ・設定永続化・`.flightcut/` 初期化
- [ ] ルート配下の再帰走査 → ffprobe メタ抽出 → DB 登録（`MediaService`）
- [ ] サムネイル生成（`ThumbService`）
- [ ] Library View（グリッド/リスト、フィルタ、検索、区間数バッジ）
- [ ] プレビュー再生（`<video>`）

### Phase 2 — 区間 & 書き出し（MVP ゴール、未着手）
- [ ] キーフレーム抽出・キャッシュ・タイムライン可視化
- [ ] 区間ブックマーク CRUD、スナップ（前/後キーフレーム）
- [ ] ロスレス書き出し（`ExportService`、stream copy、単発 / バッチ）
- [ ] Export Queue（出力先 / 命名テンプレート / 進捗 / ログ）

### Phase 3 — ドローン強化 & 拡張（未着手）
- [ ] `flight_group` グルーピング、（任意）smart-cut、（任意）解析サイドカー、（任意）跨ぎ書き出し

## 注意点・申し送り

- **ルートフォルダはハードコードしない**。ユーザー指定を必須とする。
- 開発用サンプルデータ: `E:\sample files\videos-swiss-drone-2026`（`2026-6-16/` 等の日付サブフォルダ配下に素材）。既定値に埋め込まない。
- 着手前に [plan.md](plan.md) の「オープンな設計判断」5点を詰めること（特にプレビューのプロキシ要否）。
- ロスレス書き出しの ffmpeg 引数の罠（`-ss` の位置、`-t` vs `-to`）は [../spec.md](../spec.md) §6.3 を参照。
